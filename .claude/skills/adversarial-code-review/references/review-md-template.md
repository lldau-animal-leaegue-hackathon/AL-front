# <대상> 코드리뷰 결과 (YYYY-MM-DD)

> 대상: <브랜치> (비교 기준 `main` 대비 N커밋) — <도메인> 현존 N파일.
> **기준 커밋: `<해시>`** (모든 파일:라인 번호는 이 시점 기준 — 코드가 진행됐다면 라인이 밀렸을 수 있으니 심볼명으로 재탐색할 것).
> 방법: 파인더 N(전량 정독 + 원본 대비 회귀 추적) → 중복 제거 → 발견별 적대 검증 M(반박 기본 스탠스, 재현 시나리오 필수).
> 통계: 원시 N → **확정 M** (major a · minor b) / 반박 k.
> 사전 제외(의도된 결정 — 수정 세션도 건드리지 말 것): <목록 — `review-workflow.js`의 KNOWN_INTENTIONAL 참조>.
> 관련: <로드맵/이전 리뷰 상대 링크>

## 이어받는 세션을 위한 작업 규칙

- **커밋은 사용자가 명시 요청할 때만.** 수정 묶음이 끝나면 커밋 메시지 후보 + 테스트 가이드만 자동 제시. push는 사용자가 직접.
- <항목별 선행 스킬 발동 지점 — 예: M2 수정 전 `backend-dto-check`로 응답 클래스 필드 재확인>.
- 리팩토링성 수정 후에는 `refactor-equivalence-check` 발동.
- **테스트 러너 없음** — 검증은 ①`npm run check`(typegen→tsc→eslint→prettier) ②`npm run build`(라우트 표 전후 비교) ③dev 서버 수동 확인 ④DevTools 네트워크 탭.
- `npm run dev`는 **백그라운드로만** 실행하고 확인 후 종료한다.
- 수동 검증 진입 경로 지도: <발견 그룹별 — 예: 점수 M1~M3 = 로그인 → 메인 → 점수판>.
- **결정 필요 항목(사용자에게 물어볼 것)**: <목록 — 옵션 병기>. 나머지는 결정 없이 착수 가능.
- 각 항목 수정 완료 시 체크박스 갱신 + 커밋되면 해시를 항목 옆에 기록.

## 🔴 Major (a) — <심각도 기준: 데이터 정합성·보안·리소스 누수 등>

- [ ] **M1. <제목>** — `<파일>:<라인>`
      <증상 + 실패 시나리오(구체 입력→잘못된 결과)>. <기존 버그면 "(main에도 있던 기존 버그)" 표기>
      → 수정: <방향>.

## 🟡 Minor (b)

| #   | 위치            | 요약                     |
| --- | --------------- | ------------------------ |
| m1  | `<파일>:<라인>` | <한 줄 증상 + 수정 방향> |

## 반박 N건 부기 (기각됐지만 기록할 것)

- <검증자가 기각한 사유 + 그래도 남는 컨벤션 위반(any 사용·hex 하드코딩 등)>

## 권장 수정 순서

1. <묶음 — 이유(실해 크기·수정 크기)>

## 진행 상태

- [x] 리뷰 실행 + 적대 검증 (YYYY-MM-DD)
- [ ] 수정 착수 — <완료 시: 날짜 + 분할 커밋 해시(도메인별) + 사용자 결정 인용>
