# 이 파일을 복사해서 .env.local 을 만들고 실제 값을 채워 넣으세요.
#   Windows(PowerShell): Copy-Item .env.example .env.local
#   macOS / Linux:       cp .env.example .env.local
#
# .env.local 은 .gitignore 에 걸려 있어 커밋되지 않습니다. 실제 키를 여기(.env.example)에 적지 마세요.
#
# 규칙: NEXT_PUBLIC_ 접두사가 붙은 값은 브라우저 번들에 그대로 노출됩니다.
#       비밀 키에는 절대 NEXT_PUBLIC_ 을 붙이지 마세요.

# --- 서버 전용 (브라우저에 노출되지 않음) ---

# next.config.ts 의 rewrites 가 /api/* 를 이 주소로 프록시합니다.
# 비워두면 프록시를 사용하지 않습니다.
BACKEND_ORIGIN=http://localhost:8080

# ⚠️ ANTHROPIC_API_KEY / ANTHROPIC_AUTH_TOKEN 을 설정하지 마세요.
#    /api/ai/* 는 Claude Code(`claude -p`)를 '구독 인증'으로 호출해 구독 한도에서 차감합니다.
#    API 키가 있으면 Claude 가 구독보다 키를 우선해 별도 종량 과금됩니다.
#    (src/lib/claude/runner.ts 가 자식 프로세스 환경에서도 강제로 제거합니다.)

# (선택) claude 실행 파일 경로 — PATH 에 없을 때만 지정하세요.
# CLAUDE_BIN=claude

# --- 브라우저에 노출됨 ---

# 클라이언트에서 직접 호출할 API 베이스 URL
NEXT_PUBLIC_API_BASE_URL=/api
